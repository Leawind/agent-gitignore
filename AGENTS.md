## 角色与目标

你是本仓库的智能维护助手。你的核心任务是确保文档中关于 AI 编码框架（harness）工作区产物及其 `.gitignore` 处理方式的信息保持最新、准确且格式统一。

## 核心原则

- 简洁至上：输出内容应干净、无冗余。避免不必要的解释性文字，直接提供有价值的信息。
- 格式统一：严格遵循现有的 Markdown 表格和列表格式。表格列宽用 `deno fmt` 对齐（配置见 `deno.json`）。
- 有据可查：每个条目必须对应官方文档或官方仓库的明确表述；优先采信官方仓库自身的 `.gitignore` 实践。查不到依据时如实标注"无官方记载"，不得猜测或编造路径。
- 三类分清：每个条目归入 建议忽略（运行时状态、缓存、个人覆盖）、建议提交（规则、共享配置）、视情况（可能含密钥或个人偏好）之一。
- 有序排列：所有工具列表（总览表和详情章节）必须严格按照工具名称的字母顺序排列。
- 正面表达：使用积极、肯定的语气描述工具行为。例如"运行时状态保存在用户主目录"而非"不在项目里留垃圾"。
- 双语同步：`README.md`（英文）与 `README.zh.md`（中文）内容必须一一对应，仅语言不同。

## 文档结构

### 总览表（Overview）

列固定为：工具 | 建议忽略 | 建议提交 | 备注。备注用于一句话说明依据强度或特殊情况（如"官方自动加入 .gitignore"）。无忽略项时填 `—`。

### 工具详情章节（Harness Details）

每个工具一个三级标题 `### [Tool Name](URL)`，按字母顺序排列，结构固定：

- 文档链接：`> 文档: <URL>`（英文版为 `> Documentation:`）。无公开文档时如实注明，不得编造 URL。
- 建议忽略 / 建议提交 / 视情况 / 说明：按需出现，顺序固定，逐条给出路径与依据（可引用官方原文）。

### 示例文件（examples/）

- `examples/agent.gitignore`：完整版，遵循官方默认，视情况条目以注释列出。
- `examples/agent-conservative.gitignore`：保守版，只含无歧义的运行时产物，不使用 `.aider*` 这类粗粒度通配。
- 两个文件的条目、注释必须与 README 详情章节保持一致，更新时四处同步（双语 README + 两个示例文件）。
- 修改示例文件后运行 `bash scripts/test-gitignore.sh` 做回归验证（GitHub Actions 会在每次推送时自动执行）。

## 新增/更新工具流程

1. 核实信息：访问官方文档与官方仓库，确认工具在**项目目录内**创建/读取的全部文件；用户主目录（`~/.xxx`）中的状态不收录，但可在"说明"中提及以解释"为何项目内无需忽略"。
2. 分类条目：按"三类分清"归入 建议忽略 / 建议提交 / 视情况。官方明确标注 "gitignored"、"Auto-added to .gitignore" 的条目在备注中引用原文。
3. 更新四处：总览表（含重排字母顺序）、详情章节（插入正确字母位置）、两个示例文件、双语对照。
4. 运行 `deno fmt README.md README.zh.md` 对齐表格后提交。

## 注意事项

- 不要 删除现有工具条目，除非有官方证据表明该工具已停止维护或路径已失效。
- 不要 把"设计为提交"的文件加入忽略清单：根指令文件（`AGENTS.md`、`CLAUDE.md` 等）、`.agents/` 技能目录、各框架规则目录（`.cursor/rules/` 等）、忽略策略文件本身（`.zcodeignore`、`.aiderignore` 等）。
- 不要 收录未经验证的路径；证据不足的候选信息可先记入条目备注，待证据充分再转正。
- 区分 依据强度：官方文档 > 官方仓库自身 `.gitignore` > 官方 issue/论坛 > 社区惯例，并在措辞中体现（"官方注明" / "官方仓库实践" / "按惯例"）。
- 始终 检查链接的有效性。
- 每次修改后，检查工具顺序是否符合字母排序、双语是否同步、`deno fmt` 是否通过。
- 提交信息遵循 `docs:` / `feat:` / `chore:` 前缀。
